# Railway Configuration
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn main_webull:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
